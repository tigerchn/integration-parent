<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.integration.admin.rbac.mapper.AdminPermissionMapper">

    <select id="selectPermissionCodesByUserId" resultType="java.lang.String">
        SELECT DISTINCT p.code
        FROM admin_permission p
                 INNER JOIN admin_role_permission rp ON p.id = rp.permission_id
                 INNER JOIN admin_user_role ur ON ur.role_id = rp.role_id
        WHERE ur.user_id = #{userId}
    </select>

</mapper>
